{
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "apps/vor/*"
    ]
  },
  "devDependencies": {
    "eslint": "^7.25.0",
    "husky": ">=6",
    "lint-staged": ">=10"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": "eslint --cache --fix",
    "*.go": "gofmt -w -s"
  },
  "scripts": {
    "postinstall": "husky install"
  }
}
