{
  "name": "e2e",
  "version": "1.0.0",
  "description": "End to end test of all obserfy",
  "main": "index.js",
  "license": "GPL-3",
  "private": true,
  "devDependencies": {
    "@cypress/code-coverage": "^3.5.0",
    "cypress": "^4.4.1",
    "cypress-axe": "^0.8.1",
    "eslint-plugin-cypress": "^2.10.3",
    "gatsby-cypress": "^0.3.3"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:ci": "yarn wait-on -t 300000  -lv http://localhost:8080/login && yarn cy:run --config baseUrl=http://localhost:8000"
  }
}
